package exxpoo2.com;

// Nessa classe será aplicada a lógica e criado os metodos para verficar o numero maior/*
public class Numeromaior {
	
	// Criando o metódo/*
	public int calculodenumeromaior(int num1, int num2, int num3) {
		if (num1 >= num2 && num1 >= num3) {
            return num1;
        } else if (num2 >= num1 && num2 >= num3) {
            return num2;
        } else {
            return num3;
		
	}

}
}
